import { Card, Typography } from 'antd';

const { Title, Paragraph } = Typography;

// 交易系统（策略）页面
const StrategiesPage = () => {
  return (
    <Card>
      <Title level={3}>交易系统</Title>
      <Paragraph type="secondary">交易策略与自动化系统管理（占位内容）。</Paragraph>
    </Card>
  );
};

export default StrategiesPage;
