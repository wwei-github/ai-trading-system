import { Card, Typography } from 'antd';

const { Title, Paragraph } = Typography;

// AI 助手页面
const AiPage = () => {
  return (
    <Card>
      <Title level={3}>AI 助手</Title>
      <Paragraph type="secondary">智能对话与交易辅助决策（占位内容）。</Paragraph>
    </Card>
  );
};

export default AiPage;
